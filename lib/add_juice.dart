import 'package:flutter/material.dart';

class AddJuice extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Add Juice'),
      ),
    );
  }
}
